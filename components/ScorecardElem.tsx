import React from "react";
import { View } from "react-native";
import { Hole, Teebox } from "@/constants/API";

interface Show9HoleScorcardProps {
    teebox: Teebox;
  }

export const ScorecardElement: React.FC<Show9HoleScorcardProps> = ({teebox}) => {

    return (
        <View>

        </View>
    )
}
export default ScorecardElement;